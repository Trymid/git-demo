Working directory
	git checkout --file.txt
	git ckeckout .
	git clean -xcf

Staging area (index)
	git reset --file.txt

Local branch
	git reset HEAD^^
	git commit --amend -m "commit message"

Remote repository
	git revert <sha1>


git reset --(soft,mixed or hard) HEAD~over9999+  (перекидывает на
необходимое количество коммитов назад, закидывая в стаге (софт),
без стаге и по умолчанию (микс) и удаляет вообще коммит на харде)